# Terraform local state files
*.tfstate
*.tfstate.*

# Terraform plugin cache
.terraform/

# Sensitive SSH key files
*.pem

# Optional: terraform variable files with secrets
*.tfvars

# Lock files (only ignore if you *don't* want reproducibility)
# .terraform.lock.hcl ← We want to keep this in Git

# Debug logs
crash.log

# Terraform state and cache
terraform.tfstate
terraform.tfstate.backup

# Sensitive variable definitions
terraform.tfvars

# Python cache
__pycache__/

# Zipped Lambda code (optional)
lambda_payload.zip
